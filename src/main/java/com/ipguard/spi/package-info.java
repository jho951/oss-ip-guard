/**
 * RuleSource SPI interfaces for the IP guard access-control stack.
 * <p>
 * Implementations provide raw rule text for the core engine, and built-in adapters live in {@code RuleSources}.
 */
package com.ipguard.spi;
