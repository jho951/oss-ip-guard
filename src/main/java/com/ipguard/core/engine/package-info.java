/**
 * Core decision engine for IP allow/deny evaluation.
 * <p>
 * This package contains the framework-agnostic engine that consumes parsed rules and produces decisions,
 * plus convenience factory entrypoints such as {@code IpGuards}.
 */
package com.ipguard.core.engine;
